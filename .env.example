# Draft — copy to ".env" in this directory (repo / app code root next to anki_image_updater.py).
# The real ".env" is gitignored.

# Enable round-robin API key pools from api_key_pools.json (1, true, yes, or on).
ANKI_IMAGE_UPDATER_USE_KEY_POOLS=0

# Optional: custom path to the pool file. Default: api_key_pools.json beside these sources.
# ANKI_IMAGE_UPDATER_KEY_POOL_PATH=
