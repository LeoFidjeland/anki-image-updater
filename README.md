# Install libraries

You will need to install the anki pip library

We need to do this in a virtual environment these days

```
python3 -m venv venv
source venv/bin/activate
pip3 install anki
```

after first installation, it just needs to be activated again in the folder
```
source venv/bin/activate
```

Then the scripts can be run